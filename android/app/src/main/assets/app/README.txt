# Offline app bundle

Drop the built web bundle here (output of `node ./scripts/build.mjs`, i.e. the
contents of `dist/`) so the APK can fall back to a fully offline build when the
hosted app cannot be loaded.

Expected entry point: `app/src/main/assets/app/index.html`

This file is a placeholder so the directory exists in version control.
